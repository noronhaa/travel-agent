services:
  web:
    build: .
    ports:
      - "5001:5001"
    volumes:
      - ./briefs:/app/briefs
    env_file:
      - .env
    environment:
      - FLASK_ENV=production
      - OPENROUTER_API_KEY=${OPENROUTER_API_KEY}
      - TAVILY_API_KEY=${TAVILY_API_KEY}
